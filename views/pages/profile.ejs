<%- include('../layout/head.ejs') %>
    <h1>Profile Page</h1>
    <% if (successMessage) { %>
        <div class="success-message-box" >
            <p><%= successMessage %></p>
        </div>
    <% } %>
    <p> <%= userInfo.email %>  </p>
    <div>
        <% cars.forEach(car => { %>
            <p> <%= car.name %>  </p>
        <% }) %>
    </div>
<%- include('../layout/tail.ejs') %> 